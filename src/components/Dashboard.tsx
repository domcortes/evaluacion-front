import { Card, CardContent, CardHeader } from "@mui/material";
import { Fragment } from "react";

export const Dashboard = () => (
    <Fragment>
        <br/>
        <Card>
            <CardHeader title="Bienvenido al administrador" />
            <CardContent>Dashboard de bienvenida</CardContent>
        </Card>
    </Fragment>
);